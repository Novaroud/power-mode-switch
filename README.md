# ⚡ Power Mode Switch

Простой Python-скрипт, который автоматически переключает режим питания Windows между экономным и высокой производительностью.

## 💡 Зачем это нужно

Подходит для пользователей с резервными источниками питания — ИБП, инверторов или пауэрбанков. Скрипт помогает экономить энергию при автономной работе и повышать производительность, когда питание стабильное.

## ⚙️ Как использовать

1. Скачай файл `power_mode_switch.py`.
2. Убедись, что установлен Python 3.7 или выше.
3. Запусти скрипт от имени администратора:

   ```
   python power_mode_switch.py
   ```
4. Через 3 секунды окно закроется автоматически.

## 🔍 Где взять и как изменить GUID режимов

GUID — это уникальный идентификатор схемы питания Windows. Чтобы узнать доступные GUID, открой командную строку (Win + R → cmd) и введи:

```
powercfg /list
```

Пример вывода:

```
Сбалансированный              GUID: 381b4222-f694-41f0-9685-ff5bb260df2e
Экономия энергии              GUID: a1841308-3541-4fab-bc81-f71556f20b4a
Высокая производительность    GUID: 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
```

Теперь открой `power_mode_switch.py` любым текстовым редактором и найди этот блок:

```
GUIDS = {
    "eco": "a1841308-3541-4fab-bc81-f71556f20b4a",
    "perf": "8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c"
}
```

Здесь можно заменить значения на свои GUID. Например, если хочешь, чтобы экономичный режим был кастомный, вставь его код в строку `"eco": "..."`.

## 🛠 Что можно изменить в коде

Время показа консоли:

```
time.sleep(3)
```

Измени 3 на нужное количество секунд, чтобы окно держалось дольше.

Тексты сообщений:

```
print("ТЕКУЩИЙ РЕЖИМ: ЭКОНОМНЫЙ".center(50))
print("ПЕРЕКЛЮЧЕНО НА: ВЫСОКАЯ ПРОИЗВОДИТЕЛЬНОСТЬ".center(50))
```

Можно заменить текст на свои формулировки.

GUID схем:
Редактируй словарь `GUIDS` в начале файла, чтобы задать собственные профили питания.

## 🧠 Пример вывода

```
##################################################
          ТЕКУЩИЙ РЕЖИМ: ЭКОНОМНЫЙ
     ПЕРЕКЛЮЧЕНО НА: ВЫСОКАЯ ПРОИЗВОДИТЕЛЬНОСТЬ
##################################################
```
